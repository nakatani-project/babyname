<%= form_for(@originlang) do |f| %>
  <% if @originlang.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@originlang.errors.count, "error") %> prohibited this originlang from being saved:</h2>

      <ul>
      <% @originlang.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
